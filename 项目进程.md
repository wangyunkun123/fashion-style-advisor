# 项目进程

## Phase 8 — 2026-06-25 🔧 女性用户多管线全链路修复 ✅ 已完成

**包含**
- Alice 女性测试用户穿搭生成全链路打通（9 个 Bug 修复）
- 用户上下文丢失修复（generate.py --user + set_thread_user 时序）
- 品类盲区修复（补全连衣裙/针织衫/套装/短裙/半身裙 + 验证 + 温度标注）
- 原型 CDN 路径多用户支持（OUTFITS_REL/WARDROBE_REL 动态路径）
- Profile 中英文值翻译 + Cookie 跨用户污染修复
- 主用户 Profile 数据恢复（被 Alice 数据覆盖后还原）

**代码变化**
- `wechat_control.py`: set_thread_user 时序 + 3 端点 user_id + generate.py --user + profile 翻译 + user=default 路由
- `unified_pipeline.py`: 品类补全 + 温度标注 + 连衣裙验证 + OUTFITS_DIR 动态化 + 性别感知
- `build_prototype.py`: OUTFITS_REL/WARDROBE_REL + __USER__='default' + 标签路径多用户
- `common.py`: _get_active_outfits_dir() + 三函数动态路由
- `generate.py`: sys.path.insert 项目根目录

**已知问题**
- Alice 仅 28 件单品，无短袖上衣/背心/衬衣，夏季选品受限
- 主用户 profile 曾因 Cookie 污染被覆盖（已恢复，Cookie 路由已修复）
- 风格推荐仍使用主用户风格库（男女通用），未来可扩展女性专属

**备份标签**
```bash
git checkout female-user-pipeline-fixed  # 📸 女性用户管线全链路修复
git checkout female-user-testing         # 当前开发分支
```

**包含**
- 基础设施层提取 `server_infra.py` (517行)：TaskManager + SharedState + 日志 + 信号处理 + Watchdog
- 线程安全加固：`SharedState` 统一锁替代 3 个裸全局变量
- 任务超时检测：Watchdog 后台线程每 30s 扫描，running > 5min 自动标记 error
- 启动任务恢复 + 重试队列：`recover_on_startup()` 返回 `retry_queue`，`_process_retry_queue()` 读图片→base64→重新提交分析
- 优雅关闭：SIGTERM 等待活跃任务完成（15s）+ 标记 in-flight 任务为 interrupted
- daemon.sh 增强：curl `/health` 验证 + Tailscale Funnel 自动启动 + 僵尸进程检测 + 强制kill重启
- Funnel 状态前端展示：原型页 `checkDailyStatus()` 检查 `funnel_active`，异常时显示警告
- TaskManager ID 碰撞修复：同毫秒多次调用自动追加 `_1`, `_2` 后缀
- AI 分析超时从 180s 降至 120s
- 自动化测试：TaskManager 7 个单元测试 + 基础设施 4 个 smoke test + 集成测试 7 个 round-trip（共 18 个）

**代码变化**
- 新增 `tools/server_infra.py` 517 行（TaskManager + SharedState + 信号 + Watchdog + 健康检查）
- 新增 `tools/test_task_manager.py` 188 行（7 个单元测试）
- 新增 `tools/test_health.py` 105 行（4 个 smoke test）
- 新增 `tools/test_integration.py` 185 行（7 个集成测试，mock API）
- `tools/wechat_control.py` 6264→6159 行（-105），导入基础设施层 + `_process_retry_queue()`
- `tools/wechat_control_daemon.sh` 重写（curl 健康验证 + Funnel 自动启动）
- `tools/build_prototype.py` +15 行（funnel 警告 CSS + HTML + JS）
- `outfits/_tasks/` 加入 `.gitignore`

**已知问题**
- 业务逻辑层仍在 wechat_control.py 中（`_run_add_analysis` / `run_pipeline` 等 ~2000 行待抽出）

**备份标签**
```bash
git checkout b41d41c  # 📸 优化前完整备份（6264行巨石+守护脚本+多用户）
git checkout c02e83d  # 🏗️ 基础设施提取后
git checkout 6a0eca7  # 🚀 全部优化完成（当前）
```

---

## Phase 6 — 2026-06-22 🔧 结构审计中

**包含**
- 多用户架构全面稳定性修复（7 项关键修复）
- 女性测试用户（alice）隔离部署
- 手机端 fetch 拦截器（`?user=` 自动注入）
- 天气 API 失败月份感知回退（12 个月默认值）
- `_background_push` 分步日志 + git push 3 次重试
- `_load_chat_html` 原型 staleness 检测 + 自动后台重建
- 删除单品端点多用户路径修复（5 处硬编码 → `resolve_*_dir(uid)`）

**代码变化**
- `wechat_control.py` +1390 行（多用户路由 + 错误处理 + fetch 拦截器 + staleness 检测）
- `build_prototype.py` +98 行（`__USER__` 注入 + fetch 包装器注入）
- `common.py` +48 行（多用户路径解析函数）
- 新增 `quick_tag.py` (207行), `style_scout_women.py` (121行), `user_manager.py` (100行)

**已知问题**
- `wechat_control.py` 5579 行单体文件（HTTP 路由 + 管线 + 推送 + 图片缓存混杂）
- `.git` 仓库 985MB（outfits 图片 962MB + wardrobe 237MB 全部入 Git）
- 文件名 "wechat_control" 已过时（微信交互已停用，实为主应用服务器）

**回退指令**
```bash
git checkout backup-pre-refactor-20260622
```

---

## Phase 5 — 2026-06-22 ✅ 已完成

**包含**
- 每日自动穿搭推荐（Cron 5:57 + 并发锁 + `/health` 端点增强 + 手机端状态卡片）
- 穿搭周报/月报系统（`/api/report` API + 缩略图卡片 + 风格图网格 + 周期切换）
- 衣橱图片去重清理（90 文件 + 1 档案条目，enhanced/ 396→308）
- 上传分析管线 6 坑修复（Funnel 超时/任务丢失/ID 碰撞/多件图共享/错误隐藏/映射错位）
- 新单品入库（ACC-005/006/007 手部配饰, TS-012/013 短袖上衣, SH-009 短裤）
- 周报风格识别修复（STYLE_NAMES 8→18 + 34 条关键词 + YAML frontmatter 解析）
- 微信控制守护进程脚本（`wechat_control_daemon.sh`）

**代码优化**
- `rating_analyzer.py` +75 行（weekly report + 风格关键词回退）
- `wechat_control.py` +321 行（/health /api/report 并发锁 磁盘回退 source_image）
- `build_prototype.py` +126 行（健康检查 JS + 周报卡片 + 上传错误处理）

**回退指令**
```bash
git checkout phase-5
```

---

## Phase 4 — 2026-06-18 ✅ 已完成

**包含**
- 手机端 5 Tab 完整控制台（推荐/探索/衣橱/添加/设置）
- 统一推荐管线（AB线合并 → AI主导+数据支撑+规则验证）
- 两轮接力 Seedream 抠图生图（Pass1 核心4件 → Pass2 配饰精确化）
- 微信推送与控制台内容同步（三层防御机制）
- 三级评分系统 + 偏好学习 + 单品禁用
- 场景适配规则（运动→功能鞋/面料、天气→风格+单品）
- 配色色块自动生成 + CDN 推送

**代码优化**
- 创建 `tools/common.py` 共享模块（消除 15+ 处跨文件重复定义）
- 删除 ~500 行死代码和未用导入
- wechat_control / build_prototype / unified_pipeline 内部合并精简

**回退指令**
```bash
git checkout phase-4
```

---

## Phase 3 — 2026-06-15 ✅ 已完成

**包含**
- 手机端首页大改版（Hero区 + 单品清单 + 历史推荐 + Tab Bar）
- 探索页卡片图片化（49 风格代表图 + 56×72px 竖版）
- 风格百科图库（416 张参考图，HTML 浏览页一键查看）
- 智能添加页（拍照/上传 → 豆包视觉 AI 识别 → 自动入库）
- 衣橱分析面板（品类/利用率/闲置/购买建议）
- NEW 徽标（新入库单品红色脉动标识）
- `build_prototype.py` 动态原型生成（扫描 outfits/ 自动注入数据）

**修复**
- Tab 图标消失 + 切换失效（data-page 不匹配）
- JS 语法错误导致全部事件失效（多余 `}}`）
- 手机端效果图缺失 + 穿搭重复推荐 + 排版文字显示

**回退指令**
```bash
git checkout phase-3
```

---

## Phase 2 — 2026-06-14 ✅ 已完成

**包含**
- 风格库（49篇百科 + 8指纹匹配 + 五层评分）
- 用户反馈系统（评分→缓存动态调整）
- B线风格实验室（策略驱动 + 冷门挖掘 + 场景匹配）
- 推荐优先级体系（硬阻断 > 实用 > 美观 > 舒适）
- 排版重构（左右均衡 + 顶部STYLE NOTES）
- 推送优化（CDN动态hash + URL编码 + 色块截图）

**回退指令**
```bash
git checkout phase-2-complete
```

---

## Phase 1 — 2026-06-13 ✅ 已完成

**包含**
- 基础穿搭推荐（天气+场合→风格匹配）
- 衣柜管理系统（76件单品标签）
- 微信推送（Server酱）
- 手机远程控制（ngrok + HTML面板）
- 豆包Seedream生图管线

**回退指令**
```bash
git checkout 69a70dd
```

---

## 操作速查

### 核心操作
| 操作 | 命令 |
|------|------|
| 推荐穿搭（手机端） | 控制台点 🧠推荐 / 🧪探索 |
| 推荐穿搭（命令行） | `python3 tools/unified_pipeline.py` |
| 生成效果图 | `python3 tools/generate.py <outfit_dir>` |
| 排版合成 | `python3 tools/composite_v2.py <outfit_dir>` |
| 微信推送 | `python3 tools/build_push.py <outfit_dir> --rich --no-bline --stdout` |
| 重建原型 | `python3 tools/build_prototype.py` |
| 手机控制台 | `bash tools/start_wechat_control.sh` |

### 风格库
| 操作 | 命令 |
|------|------|
| 研究风格 | `python3 tools/style_research.py <style_id>` |
| 风格排名 | `python3 tools/style_matcher.py <style_id>` |
| 自动推荐 | `python3 tools/style_matcher.py --auto <温度> <天气> <场合>` |
| 浏览图库 | `open styles_universal/references/gallery.html` |
| 发现趋势 | `python3 tools/style_research.py --discover` |

### 衣橱管理
| 操作 | 命令 |
|------|------|
| 衣橱分析 | `python3 tools/wardrobe_advisor.py --report` |
| 添加新衣服 | 放 wardrobe 目录 → 视觉识别 → 网络搜索品牌 → 标签入库 |
| 分析偏好 | `python3 tools/rating_analyzer.py --report` |
| 月度回访 | `python3 tools/monthly_checkin.py` |

### 图片资源
| 操作 | 命令 |
|------|------|
| 搜索服装图 | `python3 tools/fashion_image_search.py --query "<关键词>"` |
| 采集小红书 | `python3 tools/xiaohongshu_scraper.py --search "<关键词>"` |
| 采集 Instagram | `python3 tools/instagram_search.py --query "<关键词>"` |
| 双平台采集 | 小红书 Top5 + Instagram Top5 → 下载封面 → 写入百科 |

### 同步
| 操作 | 命令 |
|------|------|
| 同步项目 | `bash sync.sh` |

---

## 标签速查

| 标签 | 说明 |
|------|------|
| `female-user-testing` | 当前开发分支（Phase 7 ✅）|
| `6a0eca7` | Phase 7 全部优化完成（重试队列 + 集成测试 + Funnel前端）|
| `c02e83d` | Phase 7 基础设施提取（SharedState + Watchdog + 信号）|
| `b41d41c` | Phase 7 优化前完整备份（6264行巨石）|
| `phase-5` | 每日自动推荐 + 周报系统 + 衣橱清理 + 上传管线修复 |
| `phase-4` | 手机端完整实现 + 代码精简 + 推荐逻辑完善 |
| `phase-3` | 手机端三大模块 |
| `phase-2-complete` | 风格库 + 衣橱智能系统 |
| `v1.0` | 双端测试通过 + v1.0 阶段总结 |

回退到任意版本：`git checkout <标签名>`
