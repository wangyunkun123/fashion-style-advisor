# 项目进程

## Phase 10 — 2026-07-07 🔒 稳定性与安全大修 ✅ 已完成

> **对抗性审查驱动的加固**：第一性原则 + 4 个审查 agent 并行扫查 AI 生图管线 / 手机端 / 男女数据隔离 / 跨切面工程质量，修复 12 项问题（含隐私泄漏与 RCE）

### 备份与锚点
| 项目 | 值 |
|------|-----|
| 全量备份 | `/Users/rabbit/Claude code/Fashion_backup_20260707_170026`（484M，rsync 排除 .git/豆包生图）|
| 回滚锚点（修复前）| commit `a97db1d` |
| 修复提交 | commit `69312bd` |
| 涉及文件 | `wechat_control.py`、`style_lab.py`、`photo_utils.py`、`generate.py`、`.gitignore` |

### 第一批 · 隐私与数据安全 🔴
- **`/api/image` 跨用户 ACL**：`users/` 下图片校验属主 gender/uid，禁止越权窥探他人身形照/穿搭图（返回 403）
- **`get_person_photos()` 移除 fallback-to-kun**：线程上下文缺失时返回空列表，杜绝把 kun 的脸打到其他用户效果图上
- **`.gitignore` 补 `users/**/rating.json`**：从索引移除 27 个已追踪评分文件，私密评分不再上公开 GitHub
- **`apply_rating_feedback` 按用户隔离**：从 outfit_dir 反推所属用户缓存（`_resolve_user_paths`），男女评分不再串味污染主项目 SCORE_CACHE
- **管理面板遍历修正**：`for gender,g_users in reg.items(): for uid in g_users`（原 `for uid in reg` 把性别当用户名，统计恒为 0）

### 第二批 · 生错图 🟠
- **预览图性别化**：新衣预览从 profile 读真实性别/身高/身形，原硬编码「178cm 亚洲男性」导致女用户预览变男模特穿裙
- **`_pipeline_gender` 短路 bug**：`'pipeline_gender' in dir()` 恒假，导致 `--anchor` 判定失效；改为函数级统一解析并复用
- **性别默认值统一**：onboarding 缺 gender 时拒绝（400）而非静默猜测；complete 步骤以 profile 存的 gender 为权威

### 第三批 · 并发稳定性 🟡
- **`_git_lock` 全局串行锁**：串行化后台线程的 git add/commit/push，消除 `.git/index.lock` 冲突
- **profile.json 原子写**：`_update_profile`（按路径分锁 + tempfile + os.replace），手机快速连点 onboarding 不再丢字段
- （#body 限流经核查已存在 50MB 门禁，无需重复）

### 第四批 · 健壮性 🔵
- **`eval()` → AST 白名单求值器 `_safe_eval`**：杜绝 rules.json 被污染导致 RCE；已用真实规则表达式 + `().__class__` 攻击样本测试通过
- **损坏标签文件不再静默跳过**：`get_wardrobe_summary` 记录警告，避免衣服无声消失
- **Seedream API 3 次重试**：网络抖动/5xx 自动重试，4xx 参数错误不重试
- 补充 `encoding='utf-8'`

### 验证
- 4 个改动文件全部编译/语法通过
- `test_integration.py` 7/7、`test_task_manager.py` 7/7 全绿
- 实测：无上下文 `get_person_photos()` 返回空、有上下文返回 nan 3 张照片；nan/kun 评分缓存路径正确隔离；AST 求值器行为与真实规则一致且拦截 RCE
- 服务已重启（新 PID，端口 8765 正常，新代码加载）

### 待验证（下次手机端）
用 nan 重新生成约会穿搭，确认：① 日志出现 `🎯 女性用户 → 启用 --anchor`；② 效果图为女性且还原白亚麻裙细节；③ 打分只影响 nan 自己的偏好

---

## Phase 9 — 2026-06-25→26 🏗️ 男女分线架构 ✅ 已完成

> **项目最大规模重构**：从「单用户男性为主」重构为「男女双线独立运行 + 共享公共逻辑」

### 性别分离架构
```
users/{male,female}/{user_id}/    ← 数据隔离（衣橱/穿搭/Profile）
styles/{male,female}/             ← 风格指纹隔离（18♂ + 50♀）
config/style_defaults_{male,female}.json  ← 推荐策略隔离
config/beauty_direction_female.json       ← 新增：女性美妆指引
tools/                            ← 公共逻辑（男女共享，按 gender 差异化）
```

### 目录迁移
- `wardrobe/` → `users/male/kun/wardrobe/`（76件）
- `outfits/` → `users/male/kun/outfits/`（历史穿搭）
- `users/alice/` → `users/female/nan/`
- `styles/` 平面JSON → `styles/male/` + `styles/female/`
- 清理：1655 文件，~520MB

### 关键 Bug 修复
- **`resolve_user_dir()` 根因**：关键字参数 `user_id='nan'` 不命中任何分支→返回项目根目录→性别检测静默 fallback 男性。新增第三分支自动查 gender
- **`style_lab.py` 旧路径**：`STYLES_DIR` 仍指向 `styles/`（已迁移至子目录），`load_all_styles()` 返回空→舒适区分析全部失效。修复为扫描 `styles/male/` + `styles/female/`
- **人物身份保持**：`generate.py` Pass 1 prompt 描述泛型 "Asian woman"，未告知 Seedream 参考人物照为人脸基准。新增 identity clause 自动注入
- **`get_person_photos()` 上下文丢失**：后台线程不继承 `threading.local()`，`_run_pipeline_impl` 加 `set_thread_user` 调用
- **launchd 旧进程干扰**：`com.fashion.wechat-control` 自动重启旧代码→修复被覆盖。需重新启用

### 推荐系统性别感知
- `style_matcher.py`：双配置文件 + 双 fallback + `auto_suggest_style(gender)`
- `style_lab.py`：`load_all_styles()` 68风格（18♂ + 50♀）
- Explore 个性化重写：`_load_user_style_context` + `_load_all_style_fingerprints` + `_compute_wardrobe_style_affinity`
- 信号优先级：引导偏好 > 评分舒适区 > 衣橱匹配 > 趋势fallback

### 女性美妆系统
- `config/beauty_direction_female.json`：11 美学集群
- 每集群含发型/妆容/氛围（中英文）
- `build_creation_prompt()` → `beauty_direction` → 融入 seedream prompt

### 远端精简
- `main` 强制更新为最新架构（原 female-user-testing）
- 旧 `main` → `main-legacy` 备份
- 旧 tags → `-legacy` 后缀归档

### 代码变化
| 文件 | 改动 |
|------|------|
| `tools/common.py` | `resolve_user_dir()` 第三分支 |
| `tools/style_matcher.py` | 性别感知：双配置 + 双 fallback |
| `tools/style_lab.py` | `load_all_styles()`/`load_style()` 双目录扫描 |
| `tools/unified_pipeline.py` | `_is_female_early` + beauty 注入 + gender-aware 默认风格 |
| `tools/wechat_control.py` | 3 个 Explore 辅助函数 + `_get_dynamic_mode_styles` 重写 |
| `tools/generate.py` | Pass 1 人物身份保持 + `--user` 参数 |
| `tools/build_prototype.py` | Explore hint 支持 + CDN 路径 + gender 感知 |
| `config/beauty_direction_female.json` | **新增** |
| `tools/user_manager.py` | GENDER_DEFAULTS['female'] 加 hair 字段 |

### 已知问题
- `STYLE_PHOTO_MAP` 无女性条目（50 个风格回退到默认图）
- `validate_outfit()` 硬编码男性单品 ID 未泛化
- `scene_profiles.json` 无女性场景（闺蜜聚会/下午茶等）
- CLI 工具（`style_lab.py`/`sync_items.py`/`generate_thumbnails.py`）仍硬编码旧路径
- `wardrobe/enhanced/` 146 个 `Image_` 命名抠图冗余待清理
- launchd 服务待重新启用
- `wechat_control.py` 仍为巨石文件（~6200 行）
- `_get_persona_description()` `shape_cn` 缺少 `petite` 体型映射

### 备份标签
```bash
git checkout main               # 📌 当前活跃分支（男女分线架构）
git checkout main-legacy        # 📦 旧架构备份（Phase 5）
git checkout female-user-pipeline-fixed  # 📸 Phase 8 Alice 管线修复
```

---

## Phase 6 — 2026-06-22 🔧 结构审计中

**包含**
- 多用户架构全面稳定性修复（7 项关键修复）
- 女性测试用户（alice）隔离部署
- 手机端 fetch 拦截器（`?user=` 自动注入）
- 天气 API 失败月份感知回退（12 个月默认值）
- `_background_push` 分步日志 + git push 3 次重试
- `_load_chat_html` 原型 staleness 检测 + 自动后台重建
- 删除单品端点多用户路径修复（5 处硬编码 → `resolve_*_dir(uid)`）

**代码变化**
- `wechat_control.py` +1390 行（多用户路由 + 错误处理 + fetch 拦截器 + staleness 检测）
- `build_prototype.py` +98 行（`__USER__` 注入 + fetch 包装器注入）
- `common.py` +48 行（多用户路径解析函数）
- 新增 `quick_tag.py` (207行), `style_scout_women.py` (121行), `user_manager.py` (100行)

**已知问题**
- `wechat_control.py` 5579 行单体文件（HTTP 路由 + 管线 + 推送 + 图片缓存混杂）
- `.git` 仓库 985MB（outfits 图片 962MB + wardrobe 237MB 全部入 Git）
- 文件名 "wechat_control" 已过时（微信交互已停用，实为主应用服务器）

**回退指令**
```bash
git checkout backup-pre-refactor-20260622
```

---

## Phase 5 — 2026-06-22 ✅ 已完成

**包含**
- 每日自动穿搭推荐（Cron 5:57 + 并发锁 + `/health` 端点增强 + 手机端状态卡片）
- 穿搭周报/月报系统（`/api/report` API + 缩略图卡片 + 风格图网格 + 周期切换）
- 衣橱图片去重清理（90 文件 + 1 档案条目，enhanced/ 396→308）
- 上传分析管线 6 坑修复（Funnel 超时/任务丢失/ID 碰撞/多件图共享/错误隐藏/映射错位）
- 新单品入库（ACC-005/006/007 手部配饰, TS-012/013 短袖上衣, SH-009 短裤）
- 周报风格识别修复（STYLE_NAMES 8→18 + 34 条关键词 + YAML frontmatter 解析）
- 微信控制守护进程脚本（`wechat_control_daemon.sh`）

**代码优化**
- `rating_analyzer.py` +75 行（weekly report + 风格关键词回退）
- `wechat_control.py` +321 行（/health /api/report 并发锁 磁盘回退 source_image）
- `build_prototype.py` +126 行（健康检查 JS + 周报卡片 + 上传错误处理）

**回退指令**
```bash
git checkout phase-5-legacy
```

---

## Phase 4 — 2026-06-18 ✅ 已完成

**包含**
- 手机端 5 Tab 完整控制台（推荐/探索/衣橱/添加/设置）
- 统一推荐管线（AB线合并 → AI主导+数据支撑+规则验证）
- 两轮接力 Seedream 抠图生图（Pass1 核心4件 → Pass2 配饰精确化）
- 微信推送与控制台内容同步（三层防御机制）
- 三级评分系统 + 偏好学习 + 单品禁用
- 场景适配规则（运动→功能鞋/面料、天气→风格+单品）
- 配色色块自动生成 + CDN 推送

**代码优化**
- 创建 `tools/common.py` 共享模块（消除 15+ 处跨文件重复定义）
- 删除 ~500 行死代码和未用导入
- wechat_control / build_prototype / unified_pipeline 内部合并精简

**回退指令**
```bash
git checkout phase-4-legacy
```

---

## Phase 3 — 2026-06-15 ✅ 已完成

**包含**
- 手机端首页大改版（Hero区 + 单品清单 + 历史推荐 + Tab Bar）
- 探索页卡片图片化（49 风格代表图 + 56×72px 竖版）
- 风格百科图库（416 张参考图，HTML 浏览页一键查看）
- 智能添加页（拍照/上传 → 豆包视觉 AI 识别 → 自动入库）
- 衣橱分析面板（品类/利用率/闲置/购买建议）
- NEW 徽标（新入库单品红色脉动标识）
- `build_prototype.py` 动态原型生成（扫描 outfits/ 自动注入数据）

**修复**
- Tab 图标消失 + 切换失效（data-page 不匹配）
- JS 语法错误导致全部事件失效（多余 `}}`）
- 手机端效果图缺失 + 穿搭重复推荐 + 排版文字显示

**回退指令**
```bash
git checkout phase-3-legacy
```

---

## Phase 2 — 2026-06-14 ✅ 已完成

**包含**
- 风格库（49篇百科 + 8指纹匹配 + 五层评分）
- 用户反馈系统（评分→缓存动态调整）
- B线风格实验室（策略驱动 + 冷门挖掘 + 场景匹配）
- 推荐优先级体系（硬阻断 > 实用 > 美观 > 舒适）
- 排版重构（左右均衡 + 顶部STYLE NOTES）
- 推送优化（CDN动态hash + URL编码 + 色块截图）

**回退指令**
```bash
git checkout phase-2-complete-legacy
```

---

## Phase 1 — 2026-06-13 ✅ 已完成

**包含**
- 基础穿搭推荐（天气+场合→风格匹配）
- 衣柜管理系统（76件单品标签）
- 微信推送（Server酱）
- 手机远程控制（ngrok + HTML面板）
- 豆包Seedream生图管线

**回退指令**
```bash
git checkout 69a70dd
```

---

## 操作速查

### 核心操作
| 操作 | 命令 |
|------|------|
| 推荐穿搭（手机端） | 控制台点 🧠推荐 / 🧪探索 |
| 推荐穿搭（命令行） | `python3 tools/unified_pipeline.py` |
| 生成效果图 | `python3 tools/generate.py <outfit_dir>` |
| 排版合成 | `python3 tools/composite_v2.py <outfit_dir>` |
| 微信推送 | `python3 tools/build_push.py <outfit_dir> --rich --no-bline --stdout` |
| 重建原型 | `python3 tools/build_prototype.py` |
| 手机控制台 | `bash tools/start_wechat_control.sh` |

### 风格库
| 操作 | 命令 |
|------|------|
| 研究风格 | `python3 tools/style_research.py <style_id>` |
| 风格排名 | `python3 tools/style_matcher.py <style_id>` |
| 自动推荐 | `python3 tools/style_matcher.py --auto <温度> <天气> <场合>` |
| 浏览图库 | `open styles_universal/references/gallery.html` |
| 发现趋势 | `python3 tools/style_research.py --discover` |

### 衣橱管理
| 操作 | 命令 |
|------|------|
| 衣橱分析 | `python3 tools/wardrobe_advisor.py --report` |
| 添加新衣服 | 放 wardrobe 目录 → 视觉识别 → 网络搜索品牌 → 标签入库 |
| 分析偏好 | `python3 tools/rating_analyzer.py --report` |
| 月度回访 | `python3 tools/monthly_checkin.py` |

### 图片资源
| 操作 | 命令 |
|------|------|
| 搜索服装图 | `python3 tools/fashion_image_search.py --query "<关键词>"` |
| 采集小红书 | `python3 tools/xiaohongshu_scraper.py --search "<关键词>"` |
| 采集 Instagram | `python3 tools/instagram_search.py --query "<关键词>"` |
| 双平台采集 | 小红书 Top5 + Instagram Top5 → 下载封面 → 写入百科 |

### 同步
| 操作 | 命令 |
|------|------|
| 同步项目 | `bash sync.sh` |

---

## 标签速查

| 标签 | 说明 |
|------|------|
| `main` | 📌 当前活跃分支（Phase 9 男女分线架构）|
| `main-legacy` | 📦 旧架构备份（Phase 5）|
| `female-user-pipeline-fixed` | 📸 Phase 8 Alice 管线修复快照 |
| `phase-5-legacy` | 旧架构：每日自动推荐 + 周报系统 |
| `phase-4-legacy` | 旧架构：手机端完整实现 + 代码精简 |
| `phase-3-legacy` | 旧架构：手机端三大模块 |
| `phase-2-complete-legacy` | 旧架构：风格库 + 衣橱智能系统 |
| `v1.0-legacy` | 旧架构：双端测试通过 |

> 所有 `*-legacy` 标签指向旧架构（Phase 1-5），仅供历史参考。
> 当前活跃代码在 `main` 分支。
