# 项目资产索引

> 原项目：Fashion Style Advisor  
> 用途：新项目「穿搭微调教练」可从此索引快速定位可复用资产  
> 创建于 2026-06-29

---

## 总览

```
原项目 Fashion/（约 748 文件，~43 万行）
    │
    ├─ 🧠 知识资产  ← 最核心，可直接复制
    ├─ 🔧 代码资产  ← 可提取函数/模块复用
    ├─ 📊 数据资产  ← 用户数据、风格指纹、配置
    ├─ 🎨 前端资产  ← 原型 UI 可参考
    └─ 🗑️ 可抛弃    ← 与新方向无关
```

---

## 一、🧠 知识资产（可直接复用）

### 1.1 风格百科库 ⭐⭐⭐⭐⭐

**路径**：
- `styles_universal/`（51 个男装/通用风格 × 完整百科）
- `styles/female/`（50 个女性风格 × 完整百科，WF-01 ~ WF-50）
- `styles/male/`（18 个男性风格评分指纹 JSON）

**对新产品的用途**：
- 技巧库的风格适配说明（同一技巧在 Clean Fit vs City Boy 中的推荐方式不同）
- 身形修饰知识（什么技巧修饰什么身形问题）
- 风格教育内容（Layer 2 解锁后教用户理解风格）

**结构**：每个风格目录包含：
- `encyclopedia.md` — 完整百科（历史/文化/美学/品牌/搭配建议）
- `encyclopedia.html` — HTML 版本
- `gallery/` — 参考图库（405 张图片）

**列出所有风格**：
```
日系：city_boy, amekaji, urahara, wabi_sabi, yama, techwear
韩系：clean_fit, light_mature, kpop_street, minimal
美式：ivy_league, preppy, workwear, western, streetwear
英式：mod, punk, savile_row, heritage
法意：parisian_chic, sprezzatura, pitti_uomo
北欧/澳洲：scandi_minimalism, australian_surf_casual
中式：heritage, heritage_luxe, new_traditional
当代：athleisure, smart_casual, genderless, gorpcore
复古：90s_hiphop, grunge, rockabilly
前卫：avant_garde, deconstructed
新兴：poetcore, whimsymaxxing, rugged_comfort, detention_core, 
      quarter_zip_revival, torpedo_sneakers, regency_romantic, rugged_luxury
其他：resort_vacation, blokecore, cocktail, tenniscore
```

**提取建议**：直接复制整个目录到新项目。

---

### 1.2 风格评分指纹 ⭐⭐⭐⭐

**路径**：
- `styles/male/` — 19 个男性风格 JSON
- `styles/female/` — 50 个女性风格 JSON（WF-01 ~ WF-50）

**对新产品的用途**：
- 每个指纹包含：`silhouette`、`color_rules`、`pattern`、`fabric`、`hard_constraints`、`soft_constraints`、`key_items`、`body_modifier_bonus`
- 可用于技巧推荐引擎——根据不同风格的审美偏好，推荐不同的微调方式
- 可用于 Layer 2 穿搭方案生成

**关键文件示例**：
```
styles/male/clean_fit.json          ← Clean Fit 完整评分指纹
styles/male/japanese_city_boy.json  ← City Boy 完整评分指纹
styles/female/WF-01_french_effortless/fingerprint.json  ← 法式慵懒指纹
```

**提取建议**：直接复制。需要统一命名规范（男性用 `M-` 前缀，女性用 `W-` 前缀）。

---

### 1.3 产品思考与设计文档 ⭐⭐⭐⭐⭐

**路径**：
- `产品思考.md` — 完整的产品思考过程、方向探索、决策记录
- `产品设计文档.md` — 新产品完整功能规格

**提取建议**：直接复制到新项目根目录。

---

### 1.4 风格研究 Agent 知识 ⭐⭐⭐

**路径**：
- `styles_women/_shared/east_asian.md` — 东亚风格集群共享知识
- `styles_women/_shared/european_classic.md` — 欧洲经典集群共享知识
- `styles_reserve/` — 未使用的附加风格数据

**提取建议**：可参考品牌矩阵和文化背景。

---

### 1.5 Seedream Prompt 优化知识 ⭐⭐⭐⭐

**路径**：`config/seedream_prompt_optimization.md`

**对新产品的用途**：BEFORE/AFTER 效果图生成的 prompt 工程参考。

**提取建议**：直接复制文件。

---

## 二、🔧 代码资产（可提取复用）

### 2.1 AI 调用层 ⭐⭐⭐⭐⭐

| 文件 | 可提取内容 | 新用途 |
|------|-----------|--------|
| `tools/ai_api.py` | 豆包视觉 API 调用封装 | AI 审稿引擎的 VLM 调用 |
| `tools/ai_api.py` | `extract_json_from_response()` | 从 VLM 输出中提取结构化数据 |

**核心函数**：
```python
call_doubao_chat(messages, model, max_tokens, temperature)  # 主调用
call_doubao_vision(prompt, images, ...)                      # 视觉调用
```

**提取建议**：复制 `ai_api.py`，改 API 配置路径即可。

---

### 2.2 身形分析系统 ⭐⭐⭐⭐⭐

| 文件 | 可提取内容 | 新用途 |
|------|-----------|--------|
| `tools/photo_utils.py` | 身形照片处理（加载/抠图/构图） | 首次设置的 3 张照片分析 |
| `profile/analysis.md` | 身形分析方法论 | AI 身形报告的 prompt 模板 |

**提取建议**：提取关键函数，简化管线（新产品不需要抠图，只需要身形参数提取）。

---

### 2.3 服装分析系统 ⭐⭐⭐⭐

| 文件 | 可提取内容 | 新用途 |
|------|-----------|--------|
| `tools/clothing_analyzer.py` | 服装三引擎分析（YOLO + HuggingFace + OpenClip） | 每日照片中的穿搭解析 |
| `tools/cloth_parser.py` | SegFormer 服装分割 | 从用户照片中提取单品区域 |
| `tools/ai_api.py` | 豆包 VLM 分析 prompt（品类/颜色/面料） | 审稿引擎的穿搭解析环节 |

**关键 prompt（wechat_control.py 中的 VLM 分析 prompt）**：
- ~2500 字的服装分析 prompt
- 品类/颜色/品牌/面料/廓形/图案/风格/场景 全套识别

**提取建议**：提取 VLM prompt 模板，简化分析维度（新产品只需品类+颜色+版型+怎么穿的）。

---

### 2.4 Seedream 生图管线 ⭐⭐⭐⭐⭐

| 文件 | 可提取内容 | 新用途 |
|------|-----------|--------|
| `tools/generate.py` | Seedream API 调用 + 参数配置 | 生成 BEFORE/AFTER 对比效果图 |
| `config/seedream.json` / `seedream.local.json` | API 密钥和端点配置 | 直接复用 |

**提取建议**：提取核心生图函数，简化 prompt 策略（新产品主要是"微调后"效果图）。

---

### 2.5 图片处理工具 ⭐⭐⭐⭐

| 文件 | 可提取内容 | 新用途 |
|------|-----------|--------|
| `tools/auto_orient.py` | AI 方向检测（领口朝向判断） | 用户照片预处理 |
| `tools/enhance_clothing.py` | 图片增强（降噪/锐化/对比度/白平衡） | 用户照片质量提升 |
| `tools/extract_clothing.py` | Seedream 白底重构 + 去白底 | 单品提取（被动衣橱数字化用） |
| `tools/white_balance.py` | 白平衡校正 | 照片色彩还原 |
| `tools/generate_thumbnails.py` | 缩略图生成 | 历史照片缩略图 |

**提取建议**：保留方向检测 + 白平衡 + 缩略图。白底重构可以降级使用。

---

### 2.6 风格匹配 / 评分引擎 ⭐⭐⭐

| 文件 | 可提取内容 | 新用途 |
|------|-----------|--------|
| `tools/style_matcher.py` | 单品-风格兼容度计算 | Layer 2 穿搭方案评分 |
| `tools/style_scorer.py` | 批量风格评分 + 矩阵 | 风格推荐排序 |
| `tools/style_lab.py` | B 线探索逻辑 | 微调策略的探索性选择 |
| `tools/wardrobe_cluster.py` | 风格聚类推荐 | Layer 2 穿搭方案生成 |

**提取建议**：评分引擎的硬约束/软约束/颜色规则逻辑保留，但需要适配"无衣橱"模式——不是匹配真实单品，而是匹配"理想单品模板"。

---

### 2.7 天气与场景 ⭐⭐⭐

| 文件 | 可提取内容 | 新用途 |
|------|-----------|--------|
| `tools/weather_advisor.py` | wttr.in 天气 API + 场景映射 | 每日穿搭方案的温度/天气适配 |
| `config/style_defaults_male.json` | 天气 x 场合 → 风格推荐矩阵 | 温度对微调建议的影响（天热→推荐露踝，天冷→推荐叠穿） |
| `config/scene_profiles.json` | 场景画像（必备品类/面料/正式度） | Layer 2 场合适配 |

**提取建议**：天气 API 直接复用。场景映射需要在技巧库中增加天气维度（什么天气适合教什么技巧）。

---

### 2.8 评分分析 / 偏好学习 ⭐⭐⭐

| 文件 | 可提取内容 | 新用途 |
|------|-----------|--------|
| `tools/rating_analyzer.py` | 评分分析 + 月度报告 | 用户微调接受/拒绝的偏好学习 |
| `tools/history.py` | JSON 操作历史记录 | 用户行为日志 |
| `tools/intent_parser.py` | 自然语言意图解析 | 用户聊天输入解析（可选功能） |

**提取建议**：rating_analyzer 的逻辑从"穿搭评分"改为"微调接受率"分析。

---

### 2.9 公共工具 ⭐⭐⭐⭐

| 文件 | 可提取内容 | 新用途 |
|------|-----------|--------|
| `tools/common.py` | 品类代码定义、路径解析、多用户路由 | 基础数据结构 |
| `tools/user_manager.py` | 用户 CRUD + 目录初始化 | 多用户系统 |
| `tools/server_infra.py` | 日志、路径常量 | 后端基础设施 |
| `tools/image_cache.py` | LRU 图片缓存 | 前端图片服务 |

**提取建议**：品类代码（TS/LS/SHIRT 等）可直接复用。多用户架构可从文件系统改为数据库。

---

### 2.10 排版 / 展示 ⭐⭐

| 文件 | 可提取内容 | 新用途 |
|------|-----------|--------|
| `tools/composite_v2.py` | ACOC Lookbook 排版 | 周报/月报的穿搭进化展示 |
| `tools/generate_encyclopedia_html.py` | MD→HTML 百科生成 | 技巧库的 HTML 展示 |

**提取建议**：排版逻辑降级使用（新产品重点是效果图对比，不是 Lookbook）。

---

## 三、📊 数据资产

### 3.1 用户数据 ⭐⭐⭐

**路径**：`users/` 目录

| 用户 | 数据量 | 可提取内容 |
|------|--------|-----------|
| `male/kun` | 81 件衣橱 + 35 套搭配 | 身形数据、风格偏好演进、评分数据 |
| `female/nan` | 29 件衣橱 + 8 套搭配 | 女性身形数据、风格偏好数据 |

**对新产品的用途**：
- 作为种子数据验证 AI 审稿引擎
- 身形分析算法的测试集
- 微调建议的 ground truth 验证

**提取建议**：脱敏后保留身形参数和偏好数据，删除原始照片。

---

### 3.2 配置与规则 ⭐⭐⭐

| 文件 | 可提取内容 | 新用途 |
|------|-----------|--------|
| `config/category_aliases.json` | 品类中英别名映射 | AI 识别品类的 fuzzy 匹配 |
| `config/recommendation_rules.json` | 推荐规则引擎 | Layer 2 推荐规则参考 |
| `config/avoidance_rules_v2.md` | 避免规则 | 微调建议的安全边界 |
| `config/wardrobe_snapshot_202606.json` | 衣橱快照 | 测试数据 |

**提取建议**：category_aliases 直接复用。

---

### 3.3 采集的外部内容 ⭐⭐

| 路径 | 内容 | 新用途 |
|------|------|--------|
| `xiaohongshu_data/` | 小红书采集的穿搭帖子 | 技巧示范的真人素材 |
| `ig_results/` | Instagram 采集图片 | 技巧示范的真人素材 |

**提取建议**：可作为技巧卡中的真人示范素材。

---

## 四、🎨 前端资产（可参考）

### 4.1 原型 UI ⭐⭐

| 文件 | 用途 | 可参考内容 |
|------|------|-----------|
| `prototype/mobile-v2.html` | 完整移动端 UI 原型 | Tab Bar 设计、毛玻璃效果、Segmented Control、打卡 UI |
| `prototype/gender_gate.html` | 性别选择页 | 入门向导交互 |
| `prototype/icons-set.html` | 83 个自定义图标 | 服装品类图标 |
| `prototype/icons-tab.json` | Tab Bar 图标配置 | 图标映射 |

**提取建议**：UI 交互模式可参考（如照片上传 + AI 分析 + 结果展示的流程），但整体需重做以适应新产品。

---

## 五、🗑️ 可抛弃的

| 资产 | 原因 |
|------|------|
| `tools/wechat_control.py`（7225行） | 微信控制核心，与新产品无关 |
| `tools/build_prototype.py` | 旧原型构建工具 |
| `tools/composite_v2.py`（完整 Lookbook 排版） | 新产品不需要完整排版 |
| `tools/unified_pipeline.py` | 旧推荐管线 |
| `tools/xiaohongshu_scraper.py` | 小红书采集（新产品初期不需要） |
| `tools/instagram_search.py` | Instagram 采集（新产品初期不需要） |
| `tools/fashion_image_search.py` | 通用图片搜索（新产品初期不需要） |
| `tools/style_research.py` / `style_research_agent.py` | 风格研究工具（新产品不需要新增风格） |
| `tools/fetch_style_images.py` | 风格图片抓取 |
| `tools/style_image_scout.py` | 风格图片侦察 |
| `tools/classify_statement_pieces.py` | Statement Pieces 分类 |
| `tools/complete_clothing.py` | 服装补全（新产品不需要） |
| `tools/quick_tag.py` | 快速入库（新产品不需要手动入库） |
| `tools/sync_items.py` | 同步单品 |
| `tools/wardrobe_advisor.py` | 衣橱分析报告（旧逻辑） |
| `tools/generate.py`（完整穿搭生成） | 保留核心 API 调用，丢弃完整搭配生成 prompt |
| `scripts/migrate_data.py` | 一次性迁移脚本 |
| `config/beauty_direction_female.json` | 旧产品逻辑 |
| `config/explore_strategies.json` | 旧探索策略 |
| `config/garment_templates_female.json` | 旧服装模板 |
| `config/mcporter.json` | 旧配置 |
| `config/new_items.json` | 旧新单品配置 |
| `config/scene_prefs.json` | 旧场景偏好 |
| `config/style_lab_state.json` | 旧风格实验室状态 |
| `outfits/` | 旧穿搭任务队列 |
| `devlog/` / `logs/` | 旧开发日志 |
| `profile/` | 旧身型分析（仅 kun 的数据） |
| `prototype/mobile-v2.html`（整体） | UI 全替换，仅参考交互模式 |
| `wardrobe/` | 旧衣橱结构 |
| `node_modules/` | npm 依赖 |
| `fonts/` | Cormorant Garamond（新产品字体待定） |

---

## 六、新项目推荐目录结构

```
新项目/
├── 产品思考.md              ← 从原项目复制
├── 产品设计文档.md           ← 从原项目复制
├── 项目资产索引.md           ← 本文件
│
├── knowledge/               ← 知识资产（从原项目复制）
│   ├── styles/              ← styles_universal/ 全部 51 风格 + styles/female/ 50 风格
│   ├── fingerprints/        ← styles/male/ + styles/female/ 评分指纹
│   ├── style_shared/        ← styles_women/_shared/ 集群知识
│   └── prompt_guide.md      ← config/seedream_prompt_optimization.md
│
├── core/                    ← 核心引擎（从原项目提取+重写）
│   ├── ai_api.py            ← 豆包 API 调用（从 tools/ai_api.py）
│   ├── image_gen.py         ← Seedream 生图（从 tools/generate.py 提取）
│   ├── body_analysis.py     ← 身形分析（从 tools/photo_utils.py + profile/ 提取）
│   ├── outfit_parser.py     ← 穿搭解析（从 VLM prompt 提取）
│   ├── style_engine.py      ← 风格评分（从 tools/style_matcher.py 简化）
│   ├── weather.py           ← 天气 API（从 tools/weather_advisor.py）
│   ├── skill_tree.py        ← 技巧库引擎（全新）
│   └── recommendation.py    ← 推荐引擎（全新，Layer 2）
│
├── utils/                   ← 工具函数（从原项目提取）
│   ├── common.py            ← 品类代码等（从 tools/common.py）
│   ├── image_process.py     ← 图片处理（auto_orient + white_balance + enhance）
│   ├── user_manager.py      ← 用户管理（从 tools/user_manager.py）
│   ├── history.py           ← 行为记录（从 tools/history.py）
│   └── rating.py            ← 评分分析（从 tools/rating_analyzer.py）
│
├── config/                  ← 配置文件
│   ├── category_aliases.json    ← 从原项目复制
│   ├── avoidance_rules.json     ← 从原项目简化
│   ├── style_defaults.json      ← 从原项目天气映射
│   └── seedream.json            ← API 配置
│
├── data/                    ← 数据
│   ├── skill_library/       ← 技巧库（全新构建）
│   ├── test_users/          ← 种子用户测试数据
│   └── reference_photos/    ← 技巧示范图
│
├── frontend/                ← 前端（全新开发）
│   └── app/
│
└── tests/                   ← 测试
```

---

## 七、提取优先级排序

### Phase 1 必备（新项目启动前）

| 优先级 | 资产 | 操作 |
|--------|------|------|
| P0 | 产品设计文档 | 复制 → 按批改意见修订 |
| P0 | 产品思考文档 | 直接复制 |
| P0 | `ai_api.py` | 复制 + 适配新 prompt |
| P0 | `generate.py`（Seedream 部分） | 提取生图函数 |
| P0 | `category_aliases.json` | 直接复制 |
| P0 | `seedream.json` + `seedream.local.json` | 直接复制 |
| P0 | `photo_utils.py`（身形分析部分） | 提取身形参数计算 |

### Phase 2 需要时提取

| 优先级 | 资产 | 操作 |
|--------|------|------|
| P1 | 风格指纹 `styles/male/*.json` | 复制，用于技巧风格适配 |
| P1 | `style_matcher.py`（评分逻辑） | 提取硬约束/软约束/颜色规则 |
| P1 | `weather_advisor.py` | 复制 + 适配 |
| P1 | `clothing_analyzer.py`（VLM prompt） | 提取穿搭解析 prompt 模板 |
| P1 | 小红书 + Instagram 采集数据 | 复制参考图 |

### Phase 3 扩展时考虑

| 优先级 | 资产 | 操作 |
|--------|------|------|
| P2 | `rating_analyzer.py` | 适配为微调接受率分析 |
| P2 | `user_manager.py` | 复制 + 改数据库版 |
| P2 | `extract_clothing.py`（白底重构） | 降级使用 |
| P2 | `composite_v2.py`（排版逻辑） | 降级用于周报生成 |
