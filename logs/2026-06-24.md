# 2026-06-24 操作日志 — 上传管线深度修复 + 皮肤检测三版迭代 + 图片去重

## 概述
Alice 用户上传帽子/鞋子等单品图时反复被误判为真人穿搭，触发 SegFormer + Seedream 白底重构，浪费 API 配额且识别错误。经深入排查，暴露了三个独立问题：(1) 皮肤检测公式对暖色材质误判，(2) iOS Safari 文件缓存导致旧图反复上传，(3) EXIF 方向未校正。三次迭代收紧皮肤检测公式，重构前端文件选择器，建立两层去重体系。分支：`female-user-testing`。

---

## 一、皮肤检测公式三次迭代

### 1.1 旧公式回顾（坑8，之前已修复）
```
R>95, G>40, B>20, R>G, R>B, R-G>15, max-min>15
阈值：皮肤占比 < 3% → 单品图
```
- 对鞋子/包有效，但对暖色材质（草帽/皮革/卡其布）无效

### 1.2 第二版：收紧 RGB + 色相约束（坑11）
- **根因**：草帽 RGB (147, 159, 155)，25.8% 像素 R>G，H=37°（黄橙色），真人皮肤 H=15-25°（红橙色）
- **修复**：
```python
# R 必须显著大于 G 和 B
R > G+10, R > B+10, R-G > 20, max-min > 20
# 色相 ≤ 35° 排除黄橙色材质
H <= 35
```
- **效果**：草帽 15.8% → 0.8% ✅，真人照 18-100% 保持 ✅

### 1.3 第三版：加饱和度约束
- **根因**：红色布料 (H=1.6°, S=0.621) 触发误判，真皮肤饱和度 0.1-0.55
- **修复**：加 `S >= 0.1 && S <= 0.55`
- **效果**：红色布料 4.7% → 0.4% ✅

### 1.4 最终公式
```python
_skin = (R > 95) & (G > 40) & (B > 20) & \
        (R > G + 10) & (R > B + 10) & \
        ((R - G) > 20) & (max-min > 20) & \
        (H <= 35) & (S >= 0.1) & (S <= 0.55)
```

| 材质 | 旧公式 | +H≤35° | +S约束 | 最终 |
|------|--------|--------|--------|------|
| 真人皮肤 | 15-100% ✅ | 15-100% ✅ | 15-100% ✅ | ✅ |
| 草帽(米色) | 15.8% ❌ | 0.8% ✅ | 0.8% ✅ | ✅ |
| 红色布料 | — | 4.7% ❌ | 0.4% ✅ | ✅ |

---

## 二、iOS Safari 图片缓存问题

### 2.1 现象
同一张图 (MD5 `62aae458`) 出现在 **9 个不同批次**中。用户只选 1 张新图，系统收到 4-8 张（新图 + 旧图缓存混合）。

### 2.2 前端修复（三层）

**第 1 层：DOM 重建**
```javascript
// 旧：复用同一个 input 元素
document.getElementById('add-album-input').click()

// 新：每次打开前销毁旧元素，创建全新 input
var old = document.getElementById('add-album-input');
if(old) old.remove();
var input = document.createElement('input');
// ... 全新属性 ...
document.body.appendChild(input);
input.click();
```
iOS Safari 的 `input.value=''` 清不掉底层文件缓存，但 `removeChild` + `createElement` 能彻底重置。

**第 2 层：客户端文件名+大小+时间戳去重**
```javascript
var key = file.name + '|' + file.size + '|' + file.lastModified;
// 5 分钟内同 key 不重复添加
```

**第 3 层：服务端同批次 MD5 去重**
```python
# 同一 task 内相同 MD5 → 跳过（不去跨批次比较）
_batch_md5s = set()
if _img_md5 in _batch_md5s: continue
```

### 2.3 去重策略演进
| 版本 | 策略 | 问题 |
|------|------|------|
| v1 | 无去重 | 旧图反复上传，触发 SegFormer |
| v2 | 服务端 30min MD5 去重 | 太激进，新图也被拦截，全部 0 张处理 |
| v3 (最终) | 前端 DOM 重建 + 客户端 key 去重 + 服务端同批次去重 | 不跨批次拦截 |

---

## 三、EXIF 方向校正

### 3.1 问题
上传管线的分析阶段（皮肤检测 / SegFormer / AI 识别）未做 EXIF 旋转校正。iPhone 竖拍照片存为横图 + EXIF 旋转标记，分析阶段看到的是横躺图。

### 3.2 修复
`_run_add_analysis` 中 `PILImage.open()` 后立即调用：
```python
from PIL import ImageOps as _ImageOps
_pil_img = _ImageOps.exif_transpose(_pil_img)
```
入库阶段 (`_finalize_add_item`) 已有此处理，现在分析阶段也有了。

---

## 四、杂项

### 4.1 _incoming 目录清理
- 5 个批次产生 65+ 个重复文件
- 清理 97 个重复/过期文件，保留 22 个唯一图片

### 4.2 涉及文件
| 文件 | 修改 |
|------|------|
| `tools/wechat_control.py` | 皮肤检测三版迭代 + 同批次去重 + EXIF 校正 |
| `prototype/mobile-v2.html` | triggerAddCamera/Album DOM 重建 + 客户端去重 |
| `tools/build_prototype.py` | 同步原型模板修改 |
| `memory/upload-analysis-pipeline-fixes.md` | 新增坑11 + 饱和度和色相约束文档 |

---

## 五、关键经验

1. **皮肤检测不能只靠 RGB 绝对值**，必须加色相(Hue)排除黄橙色材质，加饱和度(Saturation)排除高饱和布料
2. **iOS Safari `<input type="file">` 的缓存问题**无法通过 `value=''` 解决，必须 `removeChild` + `createElement` 重建 DOM
3. **服务端去重不宜跨批次**，否则会误拦用户真正的新上传。去重主力应该在前端和同批次内
4. **EXIF 校正要在分析前做**，不仅仅是入库保存时

---
## [从 devlog/ 合并] 2026-06-24 补充记录

# 开发日志 2026-06-24

## 上传可靠性全面重构 + 三阶段入库管线升级

### 凌晨：上传稳定性攻坚战（17 项修复）

手机端凌晨上传频繁失败，从客户端→网络→服务端逐层排查：

**客户端**
- 压缩质量阶梯：优先 JPEG 0.7→0.5→0.3 降级，禁止发送原始文件
- 上传前大小检查：>400KB 自动压缩
- Canvas blob 压缩 → FormData 发送（避免 Funnel POST 限制）

**网络层**
- 修复 socket 超时：`self.rfile._sock` → `self.connection`（正确的 socket 对象）
- `rfile.read()` 加 30s 超时 + boundary 引号处理
- 上传超时 30s→60s（凌晨手机网络更慢）
- 上传分块读取 + 进度追踪：定位传输数据量

**服务端**
- 修复上传响应 BrokenPipe 导致前端报失败
- 修复上传阻塞：multipart 解析完整度检查
- 预览图缓存碰撞修复
- API 400 智能处理
- launchd 稳定性加固：直接管理 Python 进程 + Funnel 自动启动

**进程管理**
- 修复信号处理器死锁：`shutdown()` 在独立线程中调用
- 修复 SIGTERM 后进程僵死：`daemon_threads=True` + plist 加固

### 上午：入库管线三阶段升级

**Phase 1: 品类系统解耦 + 女性品类扩展**
- 品类代码与性别解耦（原硬编码男性品类 → 动态按性别加载）
- 女性品类扩展：DRESS(连衣裙) / SKIRT(半身裙) / JMP(连体裤) / BLOUSE(女士衬衫) / KNIT(针织衫)
- `CATEGORY_CONFIG` 统一品类配置，替代多处不一致映射
- 预览任务目录隔离：`users/{uid}/wardrobe/_incoming/` 按用户分离

**Phase 2: 多件检测 + Cloth Parsing 分割预处理**
- 集成 SegFormer-B2 (`mattmdjaga/segformer_b2_clothes`) 18 类服装语义分割
- `local_files_only=True` 修复国内 HuggingFace SSL 连接问题
- 最小面积阈值 3.0%（过滤误检噪点）
- 连通区域分析：同一品类多件独立检测

**Phase 3: 自动衣橱分类 + 风格聚类 + 入库自动推荐**
- 入库自动风格推荐（基于品类/颜色/品牌/面料匹配风格指纹）
- 自动 `claude_fit_comment` 生成：品牌 + 颜色 + 风格 + 品类 → 短名
- 品类冗余去重："跑鞋鞋子"→"跑鞋"、"短袖上衣T恤"→"短袖上衣"

### 白底提取管线（替代旧灰底补全方案）

**新增 `tools/extract_clothing.py`**
- `extract_to_white_bg()`：SegFormer 裁剪 → Seedream 整件重构到纯白背景(#FFFFFF)
- `remove_white_background()`：阈值去白底(>240) → 透明 PNG + 中性灰底 JPG
- 优于旧方案：整件重构（非局部 inpainting）+ 白底阈值去底（非复杂背景抠图）

**四类图片处理路径**
| 图片类型 | SegFormer | Seedream白底 | VLM行为 |
|:---|:---:|:---:|:---|
| 真人模特照 | ✅ 检测+裁剪 | ✅ 白底重构 | 逐件分析 |
| 干净单品图 | — 跳过 | — | 原图直送 |
| 电商商品图 | ✅ 检测 | ✅ | 忽略水印/促销 |
| 多图拼贴 | ✅ | ✅ | 只分析主体 |

### 入库确认增强
- 多件识别 checkbox 勾选：用户选择需入库的单品
- 全选/取消全选 toggle bar
- 只发送勾选项确认入库

### 缓存与显示修复
- `Cache-Control: no-cache, max-age=0` + ETag（修复 24h 缓存导致卡片显示旧图）
- 透明 PNG 保存到 `enhanced/{id}_cutout.png` + 透明缩略图重新生成
- 标签编辑自动更新 `claude_fit_comment`（品牌/颜色/风格/品类变更即时反映）

### 入库残留清理
- 原图清理：入库后删除 `_incoming/` 临时文件
- `_incoming` 泄漏修复：超过 24h 的分析 JSON + 残留图片自动清理
- ID 冲突旧图替换：同名 ID 重新入库时先删除旧文件

### 改动文件
| 文件 | 改动 |
|------|------|
| `tools/wechat_control.py` | 上传重构 + Phase 1/2/3 + 白底管线 + checkbox确认 + 缓存修复 + 残留清理 |
| `tools/extract_clothing.py` | **新增** — 白底提取 + 去白底模块（296 行） |
| `tools/cloth_parser.py` | SegFormer B2 集成 + `local_files_only=True` |
| `prototype/mobile-v2.html` | checkbox UI + 选择逻辑 + 上传压缩 |
| `config/new_items.json` | NEW 徽标注册 |
| `users/_registry.json` | 多用户注册 |

### 已知问题
- `wechat_control.py` 仍为巨石文件（~6200 行），业务逻辑层待抽出
- 调试日志（临时 POST/GET 追踪）待清理
