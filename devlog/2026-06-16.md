# 开发日志 2026-06-16

## 风格图库 + 探索页图片化 + 添加页重写（上）

### 风格图库建设
- 安装 `ddgs` 图片搜索引擎（DuckDuckGo-based）
- 编写 `tools/fetch_style_images.py`：自动搜索+质量筛选+批量下载
- 49 个风格各 1 张代表性主图 + 2-3 张备选图库
- 产出：50 张 `representative.jpg` + 152 张 `gallery/` 备选（53MB）

### 探索页卡片图片化
- `.es-icon` 蓝底文字 → 56×72px 竖版穿搭实拍图
- 有图自动显示，无图回退蓝底（向后兼容）
- 点击图片 → 全屏 lightbox 放大（`stopPropagation` 防误触）
- `_load_style_cards()` 新增 `image` 字段，自动检测 `representative.jpg`
- 同步更新 `build_prototype.py`，防止重建时回退

### 风格百科图库插入
- 修复 `generate_encyclopedia_html.py`：新增 `![图片]()` Markdown 语法支持
- 新增图片路径自动转 API 端点（相对路径 → `/api/image?f=...`）
- 编写 `tools/insert_gallery_to_encyclopedia.py`：批量插入图库到百科
- 49 篇百科文章尾部新增「📸 风格图库」章节
- 重新生成全部百科 HTML（50 个文件）

### 添加页重写（前端）
- **旧**：两个 tab（拍照/上传含子选项） → **新**：两个大卡片按钮，点击直接打开相机/相册
- 多图支持：连续拍摄 + 相册 `multiple` 多选 + 追加 + 删除
- 图片横滚条（缩略图 + ✕删除 + ➕添加更多）
- 进度指示器（复用已有 `progress-overlay` 组件）
- AI 审核卡片（品类/品牌/颜色/面料/风格/场景/建议 ID）

### 后端新增
- `POST /api/wardrobe/add` — 接收多张 base64 图片，豆包视觉 AI 异步分析
- `POST /api/wardrobe/add/confirm` — 审核确认 → 入库（ID分配→图片增强→标签JSON→档案更新）
- `GET /api/wardrobe/new-items` — NEW 徽标数据
- `POST /api/wardrobe/new-items/dismiss` — 点击消除徽标
- 6 个辅助函数：`_get_next_id` `_append_to_wardrobe_md` `_register_new_item` `_resize_image_for_api` `_finalize_add_item` `_run_add_analysis`

### 豆包视觉 AI 集成
- 复用 `call_doubao_chat()` 多模态模式（图片编码方式来自 `auto_orient.py`）
- 识别 13 个品类 × 颜色 × 品牌 × 面料 × 廓形 × 风格标签 × 场景 × 正式度
- 返回完整标签 JSON，用户审核后入库

### 新增工具脚本
- `tools/fetch_style_images.py` — DDGS 自动搜索+下载风格穿搭图
- `tools/insert_gallery_to_encyclopedia.py` — 批量插入图库到百科文章

### 文件改动
- `tools/wechat_control.py` +175 行（入库 API + 辅助函数）
- `tools/build_prototype.py` 添加页重写 + NEW 徽标 CSS
- `tools/generate_encyclopedia_html.py` +3 行（图片语法+路径转换）
- `styles_universal/*/representative.jpg` — 50 张新文件
- `styles_universal/*/gallery/` — 152 张新文件
- `styles_universal/*/encyclopedia.md` — 49 篇新增图库章节
- `styles_universal/*/encyclopedia.html` — 50 篇重新生成
