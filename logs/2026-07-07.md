# 2026-07-07 操作日志 — 风格库对称大修 + 生图三Bug修复 + 探索三模式收窄

## 概述
今日分三段推进：(1) 上午完成风格库男女对称大修（命名统一/摄影模板100全覆盖/男性指纹18→50）；(2) 排查并修复女性用户 nan 生图暴露的三个独立 Bug（摄影风格失效 / 连衣裙与上衣重复冲突 / 生成失败 KeyError 崩溃），并定位到「运行中服务进程加载旧代码」这一根因；(3) 按用户要求收窄「日常穿搭/改变自己/大胆跨界」三种探索模式的风格分配逻辑。

---

## 一、风格库男女对称大修（commit 47e06ca）
- 命名统一，摄影模板 100 全覆盖（50 男 + 50 女），0 fallback
- 男性风格指纹 18 → 50 补齐
- `styles_women/` 迁移残留清理，合并入 `styles/female/`
- 详见 memory: `style-library-symmetry-overhaul-2026-07-07.md`

---

## 二、生图三 Bug 修复

### 2.1 连衣裙与上衣重复冲突（校验漏洞）
- **现象**：户外机能风同时选了上衣 TS-005 和连衣裙 PT-004（category=连衣裙/DRESS），重复且连衣裙生图未生效
- **根因**：`validate_outfit` 缺一体式单品（DRESS/SUIT/JMP）与独立上衣/下装的互斥检查
- **修复**（`tools/unified_pipeline.py` ~2466）：
  - 新增 `onepiece_count`（DRESS/SUIT/JMP）互斥：>1 报错
  - 一体式存在时禁止再叠独立上衣/下装
  - `top_count` 补入 KNIT/BLOUSE；`bottom_count` 补入 SKIRT
- **测试**：7 真实 + 4 逻辑用例全过，单连衣裙/正常上下装无误伤

### 2.2 摄影风格失效（HARD LOCK 改写）
- **现象**：法式/机能风生图的场景、相机、胶片全按豆包自己想象走，无视 `get_photo_direction` 给的塞纳河/Portra/巴黎场景
- **诊断**：端到端测试证明 photo_direction 正确且已注入（char 810），是 AI 指令遵循问题，非代码 Bug（Lost-in-the-middle）
- **修复**（三处硬约束改写）：
  - A. `get_photo_direction` 返回文案 → 「HARD LOCK — 必须忠实还原，不可替换」，场景/器材/光影标 🔒 硬性设定
  - B. seedream_prompt 字段说明 → 删「自然改写」，加「🔒场景锁定/器材锁定，只能润色英文措辞」
  - C. user_prompt 开头「北京天气」→「当前天气（天气只判断穿着舒适度，不是拍摄地点）」，消除场景锚点误导

### 2.3 生成失败 KeyError 崩溃
- **现象**：「雨天禁止皮质鞋」校验失败后，AI 纠正重选只返回 id/category/reason，缺 name/color → `execute_outfit_plan` 拼表 `it['name']` KeyError → 崩溃（此时 rmtree 已清目录，留下半损坏 outfit 目录，异常被吞无 ERROR 日志）
- **修复**（`tools/wechat_control.py` ~2810）：新增 `_item_field()` 兜底，缺字段时从服装档案回填 name/color/category

### 2.4 根因：运行中服务加载旧代码 ⚠️
- 手机连接的 daemon（--port 8765）不自动 reload Python 模块，命令行测试用新代码（正常）但手机走旧进程
- **教训**：改管线代码后必须 kill + 重启手机服务进程
- 详见 memory: `generation-crash-and-photo-lock-fixes.md`

---

## 三、探索三模式收窄（本次核心优化）

### 优化前后对比
| 模式 | 优化前 | 优化后 | 定位 |
|---|---|---|---|
| 👔 日常穿搭 tweak | 10-13 | **6** | 偏好+舒适区，最稳 |
| 🧪 改变自己 transform | 15-19 | **8** | 邻接风格+流行趋势 |
| 🚀 大胆跨界 cross | 44-45（近全库） | **8** | 冲突风格+小众，真反差 |

### 三处改进（`tools/wechat_control.py` ~4386，section 7-9 重写）
1. **数量收窄**：`MODE_LIMIT = {'tweak':6, 'transform':8, 'cross':8}`
2. **加权排序**：`语义权重×1000 ± 衣橱亲和度`。日常/改变正向加权（越搭衣橱越靠前），跨界负向（越不搭越靠前）
3. **三模式完全互斥**：tweak_core / transform_core / cross_core 集合互斥，同一风格不重复出现

### 实测（kun，偏好日系City Boy/常春藤）
- 日常：4经典+2流行，全是偏好+衣橱高分
- 改变：7流行+1经典（新中式/Gorpcore/网球风，邻接但新鲜）
- 跨界：7小众+1经典（英式朋克/先锋前卫/日系机能，真反差）
- 三模式交集全空 ✅；nan 同样 6/8/8 ✅

### 附带确认的匹配链路结论
1. 真用了用户数据：身形+偏好+衣橱亲和度+评分全部参与打分且有区分度（kun 衣橱 korean_minimal 70.5 最高，不搭风格 40 垫底）
2. 新用户上传后自动匹配：三段兜底（完成引导用偏好 / 只传衣服算衣橱亲和度 / 零数据兜底+引导）全验证通过
3. 详见 memory: `explore-mode-style-narrowing.md`

### 服务状态
服务重启多次，最终 PID 24059 加载新代码，Tailscale Funnel 保留、手机 URL 不变。

---

## 四、待修（非紧急，已记入 系统升级建议.md）
1. seedream_prompt 场景锚点关键词校验+重试（第4层防线，偏离必被拦截）
2. R1 纠正重选提示词应重申必须返回全部 5 字段（id/category/name/color/reason）

---

## 五、日志体系统一合并（晚间）

### 问题
项目存在三处日志存储，同一日期内容不同，长期混乱：
- `devlog/` 目录（17文件，6/10~7/2）
- `logs/` 目录（10文件，6/13~7/7）
- `devlog.md`（660行大文件，6/13~6/25）

### 合并操作
- `devlog.md` 16个日期段 → 提取为独立 `logs/YYYY-MM-DD.md` 文件
- `devlog/` 目录全部内容 → 合并进 `logs/`（9个独有日期直接复制，8个重叠日期追加合并）
- 删除 `devlog/` 目录
- `devlog.md` 重写为索引文件（25行），链接全部19个日志文件
- 最终：`logs/` 19文件、2767行，覆盖 6/10~7/7

### 规范
以后每日操作统一写 `logs/YYYY-MM-DD.md`，`devlog.md` 只做索引。

---

## 六、TS-001 品类污染修复 + 男女数据隔离三层防线

### 问题
kun 的男士衣橱里出现"女士衬衣"分类，TS-001（HLA焦糖色POLO衫）被标记为 `category_code=BLOUSE`。

### 根因
commit `6dddd12`（6/16 远程操控）中 AI 将 TS-001 的标签错误改写：
- `category`: 短袖上衣 → 女士衬衫
- `category_code`: TS → BLOUSE
- `claude_fit_comment`: 加入"女士衬衫"

原因是 6/16 多用户测试期间新增女性品类，AI 在男性衣橱操作时混淆了品类。

### 修复

**TS-001 标签恢复**：
- ✅ `category_code` → `TS`
- ✅ `category` → `短袖上衣`
- ✅ `claude_fit_comment` → 恢复为"HLA焦糖色合身POLO衫"

**三层防线（防止再次发生）**：

| 层次 | 位置 | 机制 |
|------|------|------|
| 1. AI Prompt | `_run_add_analysis` | 男性用户 prompt 只列中性/男装品类，不展示 DRESS/SKIRT/JMP/BLOUSE |
| 2. 入库拦截 | `_finalize_add_item` | AI 返回女性品类时自动修正为 TS 并记日志 |
| 3. API 校验 | `/api/wardrobe/item/` POST | 男性用户设女性品类 → 400 错误拒绝 |

### 改动文件
- `users/male/kun/wardrobe/tags/TS-001.json` — 修正 category/category_code/comment
- `tools/wechat_control.py` — 三层防线（AI prompt性别感知 + 入库拦截 + API校验）
