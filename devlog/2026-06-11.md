# 开发日志 2026-06-11

## README 与文档优化
- README 补充微信推送功能说明（简化版，不暴露实现细节）
- README 项目结构图加入 `notify.py`、`composite.py`
- 新建 `系统升级建议.md`：13 条功能优化建议，分 4 大类，含优先级标记

## Git 规范
- 添加 `.gitignore` 规则：`*（用完删除）*` 匹配的临时文件不提交
- 清理误提交的临时文档

## 单品标注合成工具 `tools/composite.py`
**功能**：将 AI 效果图与原始单品图合成，在身体对应位置贴缩略图+标签

**迭代过程**：
1. v1：缩略图居中覆盖 → 遮挡面部，尺寸太小
2. v2：边缘布局（左/右/底），帽子移到左侧头部区域，放大缩略图（200-340px）
3. v3：尺寸分级（外套340 > 上衣/裤子/鞋320 > 包280 > 帽240 > 袜200 > 墨镜160），EXIF自动旋转，墨镜右侧头部

**用法**：
```bash
python3 tools/composite.py [穿搭目录]  # 默认最新
```

## 衣橱图片方向修正

### 问题
76 件服装图片中 57 张为横图（iPhone 横拍），标注图展示时方向错乱

### 解决
1. **`tools/fix_orientation.py`**：轻量工具，EXIF校正 + 横图转竖（顺时针90°）。测试发现绝大多数方向反了
2. **全部翻转180°**：修正旋转方向错误
3. **`tools/auto_orient.py`**：核心工具，调用豆包视觉模型自动判断每件衣服的真实朝向（上/下/左/右），精确旋转修正

### API 踩坑
- 初始尝试 `doubao-1.5-vision-pro-32k` → 401 无权限
- 用户提示 `doubao-seed-2.0-code` → 需用 `/api/plan/v3/chat/completions` 端点
- 该模型有推理 token 消耗，max_tokens 需设 ≥1000 避免输出被截断
- 解析模型输出需正则匹配结论句，避免"左右展开""上下颠倒"等描述词干扰

### 结果
- 76 件中 74 件 AI 自动修正成功
- 2 件墨镜（IMG_8586 / Image_20260610_0845_19_011）模型始终无法判断，待手动确认

## 双模型工作流
确立项目开发规范：
- **DeepSeek-V4**（对话）：逻辑、设计、开发、讨论
- **Doubao-Seed-2.0-Code**（API）：视觉识别、方向判断、图片理解
- 视觉任务由 DeepSeek 写代码调用 API，用户无需切换模型

## 新增文件清单
| 文件 | 用途 |
|------|------|
| `系统升级建议.md` | 功能优化建议池 |
| `tools/composite.py` | 单品标注合成 |
| `tools/fix_orientation.py` | 批量方向修正（轻量） |
| `tools/auto_orient.py` | AI视觉方向检测修正 |
| `devlog/2026-06-10.md` | 昨日开发日志 |
| `devlog/2026-06-11.md` | 今日开发日志 |

## 流程精简
- composite.py 移除已废弃的 `top` 边逻辑（帽子/墨镜已改为贴左右侧）
- 确立 auto_orient.py 为主要方向修正工具，fix_orientation.py 为轻量备选
- CLAUDE.md 补全所有新工具和 devlog 目录引用

## 卡片排版经验总结

经过多轮迭代，确立了服装卡片渲染的完整规则体系：

### 抠图
- rembg + alpha_matting 抠图，严格裁剪透明边（alpha≥200）
- 解决 `getbbox()` 含半透明边缘导致视觉不居中的关键问题

### 卡片
- 标准比例 1:1/3:4/4:3，长边约束，图片缩放×0.9留呼吸空间
- 纯白底+主题色边框+柔和阴影
- 左列右对齐、右列左对齐

### 旋转
- 帽子 0°、外套+10°、上衣-10°、其余 PCA 50%保留自然感
- 贴纸模式暂缓（抠图质量不稳定）

## 排版直角画册确立 ✅

经过Pinterest学习→6张参考图分析→多轮调试，确立了 **ACOC Lookbook 网格风** 作为标准排版方案：

### 设计规范
- **风格**：直角矩形 + 1px浅灰边框 + 无圆角无阴影，纯白底色
- **字体**：Didot 96px(英文标题) + 宋体72px(中文) + Georgia/STHeiti(正文28-32px)
- **配色**：豆包视觉API提取5色 → 底部COLOR PALETTE色块
- **布局**：左列主衣 + 中AI原图 + 右列配饰 + 底部FASHION STYLE ADVISOR
- **卡片**：1:1/3:4/4:3标准比例、严格透明裁剪(α≥200)、品类旋转规则

### 关键经验
- 抠图透明边缘必须收紧到α≥200（getbbox含半透明导致视觉偏位）
- 卡片比例仅用1:1/3:4/4:3三种
- 服装名至少留16字空间（中英混排）
- 配色从AI生图识别比从抠图取色准确（豆包视觉API）

### 工具清单
- `composite_v2.py` — 直角画册排版（主工具）
- `composite.py` — 旧版卡片排版（备用）
- `enhance_clothing.py` — rembg抠图+精修
- `auto_orient.py` — AI视觉方向检测
- `fix_orientation.py` — 轻量方向修正（备用）

## 全流程优化

### Token 消耗分析
单次全流程：~30秒 / ~1500 token（仅豆包配色识别）
- Seedream生图：0 token（图片API）
- 配色识别：~1500 token → **已加缓存**，同套只识别一次
- 微信推送：0 token

### 用户偏好
- 日常穿搭不推荐帽子/墨镜（除非强阳光/户外活动）

## 待继续
- 2 件墨镜手动确认方向
- 排版方案2待调试
- `系统升级建议.md` 中 P0 功能待开发
