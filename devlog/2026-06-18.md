# 开发日志 2026-06-18

## 推荐理由 + 穿搭技巧 + 放回主页

### 推荐理由 & 穿搭技巧
- 手机端 Hero 配色条下方新增「推荐理由」+「穿搭技巧」两个区块
- AI 自动生成：`rationale`（消费者视角总结，100-200字）+ `dressing_tips`（1-2条实操建议）
- 数据流：`build_enhanced_prompt()` → outfit.md → `scan_outfits()` → `build_rationale_html()` → 原型渲染
- Prompt 改动：`build_enhanced_prompt()` 和 `build_narrative_prompt()` JSON 输出新增 `rationale` 和 `dressing_tips` 字段
- `wechat_control.py` 新增 `_format_tips()` 辅助函数处理字符串/数组兼容
- `build_prototype.py` 新增 `_escape_html()` 和 `build_rationale_html()` 函数

### 放回主页
- 历史最爱卡片底部新增「📌 放回主页」按钮
- 纯前端 DOM 操作：复制历史穿搭的图/标题/标签/单品/配色/推荐理由到 Hero
- 点击即生效，刷新页面自动清除（不持久化，无 localStorage）
- 经历迭代：localStorage方案 → server-side `.pinned` 文件 → 最终纯前端DOM

### 5 套最爱穿搭补全
- 为 5 套 3 星穿搭补全推荐理由 + 穿搭技巧内容
- 修正风格标签：
  - `一套一套美式穿搭` → `美式休闲, Clean Fit, 黑色基础款, 格纹长裤, 皮质高帮鞋`
  - `一套晚上打网球穿的服装` → `日系CityBoy, 网球混搭, 速干叠穿, 复古运动配件, 军绿色系`
  - `今日穿搭。` → `日系CityBoy, 焦糖暖色系, 灰蓝水洗牛仔, 姜黄帆布鞋, 肌理感T恤`

### CSS 修复
- 配色圆点垂直居中：`display:inline-block` → `flex-shrink:0`，padding 对称化 `padding-top` → `padding: 10px 0`
- pinToHome 标题去「取消」文字：使用 `childNodes[0].nodeValue` 替代 `textContent`
- `.rationale-box` CSS 从手动添加到 `build_prototype.py` 模板（防止重建原型时丢失）

### 归档品修复
- 归档品 API 可见 + 删除彻底清理 5 类数据 + 操作后即时刷新
