# 2026-06-22 操作日志 — 多用户测试架构 + 男女混合引导

## 概述
完成 Fashion Style Advisor 的多用户测试基础设施，支持男女混合测试、性别选择引导、可跳过 onboarding、用户数据文件系统隔离。分支：`female-user-testing`。

---

## 一、多用户架构

### 1.1 用户管理 (`tools/user_manager.py`)
- 新增 `GENDER_DEFAULTS` 全局常量：男性 172cm/65kg/矩形，女性 160cm/55kg/梨型
- `create_user()` 初始化 13 品类衣橱模板（服装档案.md）
- 男性默认风格：`american_ivy_league`, `clean_fit`, `athleisure_sport`
- 女性默认风格：`WF-01`, `WF-05`, `WF-06`

### 1.2 用户上下文 (`tools/common.py`)
- `set_thread_user()` / `get_thread_user()` 线程级用户隔离
- `resolve_outfits_dir()` / `resolve_tags_dir()` 多用户路径解析
- `resolve_wardrobe_dir()` / `resolve_user_dir()` 用户目录工具函数

### 1.3 请求路由 (`tools/wechat_control.py`)
- `_resolve_user_from_request()` 三级优先级：URL `?user=` > Cookie `fashion_user` > default
- 主页设置 Cookie：`fashion_user={user_id}; Path=/; Max-Age=86400; SameSite=Lax`
- 默认用户清除 Cookie 防止 API 路由错误

---

## 二、5 步引导流程（Onboarding）

### 2.1 流程设计
| Step | 内容 | 可跳过 |
|------|------|--------|
| 0 | 性别选择（男/女）| ❌ 必选 |
| 1 | 身形信息（身高/体重/身形/肤色）| ✅ 智能默认 |
| 2 | 风格偏好（最多选 4 个）| ✅ 加载默认 |
| 3 | 衣橱上传 | ✅ 空衣橱 |
| 4 | 分析等待 → 完成 | — |

### 2.2 UI 统一
- Onboarding CSS 从暖棕色改为 Navy 主题（`#1e3a5f`），与 `mobile-v2.html` 统一
- 色调变量：`--navy`, `--navy-light`, `--text`, `--sub`, `--border`, `--bg`
- 性别选择卡片：👨 大图标 + 描述，点击切换

### 2.3 向下兼容
- 不带 `?user=` 参数 → `('default', False)` → 使用项目根路径，行为完全不变
- 所有原有 API 端点（`/`, `/admin`, `/api/wardrobe`）保持 200

---

## 三、工具链多用户适配

### 3.1 评分分析器 (`tools/rating_analyzer.py`)
- 新增 `--user` 参数，路径重定向到用户目录
- 修复 `sys.path` 导入问题

### 3.2 衣橱顾问 (`tools/wardrobe_advisor.py`)
- 同上 `--user` 支持

### 3.3 统推荐管线 (`tools/unified_pipeline.py`)
- TAGS_DIR 多用户路由

---

## 四、提交记录

| Commit | 内容 |
|--------|------|
| `711febc` | Phase 5 — 每日自动推荐 + 周报系统 + 衣橱清理 |
| `e8adc48` | 男女混合测试：性别选择 + 可跳过引导 + UI 统一 |

---

## 五、测试验证

- 9/9 E2E 测试通过
- 原测试链接（不带 `?user=`）全部正常
- becca 引导流程完整跑通
- 日推 `curl localhost:8765/cmd?t=今天穿什么` 200
